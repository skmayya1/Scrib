import prisma from "@/DB/prisma";
import { NextResponse } from "next/server";

export async function GET() {
  //const dele = await prisma.meetings.deleteMany();
//  return NextResponse.json(dele.count, { status: 400 });
}
